#include<stdio.h>
#include<math.h>
main()
{   int base,power;
    printf("Enter the base:");
    scanf("%d",&base);
    printf("\nEnter the power");
    scanf("%d",&power);
    printf("\n%f",pow(base,power));

}
